apiVersion: numaflow.numaproj.io/v1alpha1
kind: Pipeline
metadata:
  name: linear
spec:
  vertices:
    - name: in
      scale:
        min: 1
      source:
        generator:
          rpu: 5
          duration: 1s
    - name: cat
      scale:
        min: 1
      udf:
        container:
          image: quay.io/numaio/numaflow-go/map-cat:stable
    - name: out
      scale:
        min: 1
      sink:
        log: {}
  edges:
    - from: in
      to: cat
    - from: cat
      to: out
